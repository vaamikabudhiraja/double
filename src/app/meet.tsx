import { TandemScreen } from '@/components/tandem-screen';

export default function MeetScreen() {
  return (
    <TandemScreen
      section="meet"
      kicker="Not alone"
      blurb="Pair-to-pair doubles and couples-meet-couples. Opt-in, never swiping — deferred until the MVP is live, per CLAUDE.md."
    />
  );
}
